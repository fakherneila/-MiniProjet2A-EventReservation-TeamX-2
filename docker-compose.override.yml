version: '3.8'

services:
  php:
    user: "1000:1000"
    environment:
      - PHP_USER_ID=1000
      - PHP_GROUP_ID=1000
    volumes:
      - ./:/var/www/html
      - ./docker/php/php.ini:/usr/local/etc/php/php.ini
